# -*- coding: utf-8 -*-
"""iris_dataset.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1lDn__7PACFinESjTjrY113PYRjggyeeM
"""

import numpy as np
import matplotlib.pyplot as plt

from sklearn.datasets import load_iris

dataset = load_iris()

print(dataset.DESCR)

x = dataset.data

y = dataset.target

x.shape

y.shape

y

x

plt.plot(x[:, 0], x[:, 1], 'r.' )

plt.plot(x[:, 0][y == 0], x[:, 1][y == 0], 'r.', label = 'Setosa'  )
plt.plot(x[:, 0][y == 1], x[:, 1][y == 1], 'b.', label = 'Versicolour'  )
plt.plot(x[:, 0][y == 2], x[:, 1][y == 2], 'y.', label = 'Virginica'  )
plt.legend()
plt.show()

plt.plot(x[:, 0][y == 0]* x[:, 1][y == 0], x[:, 2][y == 0]* x[:, 3][y == 0], 'r.', label = 'Setosa'  )
plt.plot(x[:, 0][y == 1]* x[:, 1][y == 1], x[:, 2][y == 1]* x[:, 3][y == 1], 'b.', label = 'Versicolour'  )
plt.plot(x[:, 0][y == 2]* x[:, 1][y == 2], x[:, 2][y == 2]* x[:, 3][y == 2], 'y.', label = 'Virginica'  )
plt.legend()
plt.show()

from sklearn.preprocessing import StandardScaler

x = StandardScaler().fit_transform(x)

from sklearn.model_selection import train_test_split

x_train, x_test, y_train, y_test = train_test_split(x, y)

from sklearn.linear_model import LogisticRegression

log_reg = LogisticRegression()

log_reg.fit(x_train, y_train)

log_reg.score(x, y)

log_reg.score(x_test, y_test)

log_reg.score(x,y)